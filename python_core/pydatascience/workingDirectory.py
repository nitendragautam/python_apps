import os ## Imports the Operating System package
os.getcwd() # Gets the Current Directory
print(os.getcwd())
os.chdir(os.getcwd() +"/neworkdir") # Sets the working directory to new path






# working directory is the default file path that python reads or saves files
# into. An example of the working directory is ”C://file/path".
# The os library is needed to set and get the working directory.